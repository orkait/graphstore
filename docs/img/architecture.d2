direction: down

title: |md
  # graphstore — architecture
| { near: top-center }

user: User / Agent {
  shape: oval
  style.fill: "#e0f2fe"
}

dsl: |md
  ## DSL
  Lark LALR(1), 70+ commands
|

ingest: |md
  ## Ingest
  tiered · modality-aware
| {
  style.fill: "#dcfce7"
}

graph: |md
  ## Graph
  numpy columns
  scipy CSR edges
| {
  style.fill: "#dbeafe"
}

vector: |md
  ## Vector
  usearch HNSW
  cosine
| {
  style.fill: "#dbeafe"
}

document: |md
  ## Document
  SQLite + FTS5
  BM25 + blobs
| {
  style.fill: "#dbeafe"
}

remember: |md
  ## Retrieval
  REMEMBER · RECALL
  SIMILAR TO · LEXICAL
| {
  style.fill: "#fef3c7"
}

user -> dsl
dsl -> graph
dsl -> vector
dsl -> document
ingest -> graph
ingest -> vector
ingest -> document
graph -> remember
vector -> remember
document -> remember
remember -> user: ranked {style.stroke-dash: 4}
