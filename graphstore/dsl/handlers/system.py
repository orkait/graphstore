"""System commands are handled by SystemExecutor (separate from the handler registry).

This file exists as a placeholder. SYS * commands use their own dispatch in
executor_system.py and are routed via the _SYS_TYPES check in graphstore.py.
No @handles registration needed.
"""
